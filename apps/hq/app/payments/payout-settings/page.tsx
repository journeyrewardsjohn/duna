import { MoneyPageContent } from "@/components/money-page";

export const metadata = { title: "Money · Payout Settings" };

export default function MoneyPayoutSettingsPage() {
  return <MoneyPageContent view="payout-settings" />;
}
